# Proposal: Cross-Book PEOPLE.md Pattern Formalization (Emergency Amendment v3.3.2)

**Target:** RULES-CORE.md Rule 29 §People and Genealogy Files (bold-paragraph block)
**Source feedback item:** Phase 13 — Cross-Book Canonical PEOPLE (post-Tier-2 sequence)
**Lock-Protocol path:** Emergency amendment per RULES-CORE.md §AMENDMENT & LOCK PROTOCOL — bug fix / audit-driven formalization, no signed-off verses affected, additive clarification only
**Proposed for:** Ruleset v3.3.2 (point release; not a v3.4 bump)
**Status:** approved 2026-05-18 via `docs/audit/PHASE_13_PLAN.md` Q1=A / Q2=A / Q3=A / Q4=C / Q5=A decision lock

---

## Rationale

The cross-book see-only PEOPLE.md pattern has been in production use since Phase 6 (2026-05-09) and was extended by Phase 10 (John PEOPLE.md authoring, 2026-05-14) + the Possible-Content Bundle (2026-05-16, Iakobos see-only stub). Production state at 2026-05-18:

- **Domain type** (`src/domain/content/types.ts:222-223`): `PersonEntry.crossBookSee?: string` + `PersonEntry.inBook?: string`.
- **Parser** (`src/infrastructure/content/people-parser.ts:215-228`): `EXACT_LABEL_ALIASES.crossBookSee = ["see", "ver", "siehe"]` and `EXACT_LABEL_ALIASES.inBook` covering matthew/genesis/john × 4 locales (12 aliases).
- **UI** (`src/ui/people/person-card.tsx:19-50`): `CrossBookSeeField` renders active `<Link>` to `/{locale}/{slug}/people` when the target book is in `bookLabels` map; falls back to plain text for forward references to unauthored books (acts, exodus, kings, isaiah).
- **Production content**: 11 see-only stubs per locale × 4 locales = 44 stub instances. 5× Matthew → genesis (Avraham, Yitschaq, Ya'aqov, Yehudah, Tamar); 1× Matthew → acts (Iakobos); 2× John → matthew (Yochanan, Yeshua); 1× John → exodus (Mosheh); 1× John → kings (Eliyahu); 1× John → isaiah (Yeshayahu).
- **Tests** (`src/infrastructure/content/__tests__/people-parser.test.ts:411-470`): 5 cross-book test cases.

The pattern is correct and complete, BUT it is undocumented at the rules level beyond a single passing mention ("cross-book canonical entries added when multiple books exist"). The 5-change new-book activation sequence (audit-v1 §3.3) is undocumented anywhere, which creates a forward-tracking trap: future Phase 12+ authors adding a new book's PEOPLE.md will face silent parse failures if the parser-alias extension step is missed.

This amendment formalizes the convention, the markdown syntax, the locale-translation table, the allow-list of valid target slugs, and the 5-change activation checklist — so future authors find the convention where they look first.

## Affected rules / interaction with existing policy

- **Rule 29 §People and Genealogy Files block** (bold-paragraph within Rule 29 territory): existing 14-line block extended with new sub-paragraphs covering the cross-book pattern. No existing text removed or revised.
- **RULES-HB.md §PROPER-NAME TABLE** and **RULES-GS.md §PROPER-NAME TABLE — GREEK SCRIPTURES**: unchanged. The cross-book pattern references the existing proper-name tables (for locale-familiar forms) but doesn't modify them.
- **CORE Rule 17 (Name rendering policy)**: unchanged. The cross-book see-only stub still uses the established `<Translit> (<Familiar>)` heading form per the proper-name tables.
- **No existing rules are modified.** This is an additive clarification only.
- **No signed-off verses are affected.** All current PEOPLE.md content is `provisional` status.

## Draft amendment text (append to RULES-CORE.md Rule 29 §People and Genealogy Files block, after the existing "Per-person fields" and "Ethnogenesis prohibition" paragraphs)

```markdown
**Cross-Book Canonical Entries (v3.3.2 amendment, 2026-05-18):** When a person appears in multiple books, a SINGLE canonical entry lives in one book's PEOPLE.md (the "canonical home" — typically the book where the person's narrative arc is most substantive), and the OTHER books that mention the person use a **see-only stub** that references the canonical home.

**Markdown convention:**
```markdown
## <Transliterated> (<Familiar>)
**See:** <canonical-book-slug>/PEOPLE.md
**In <Current Book>:** <2-4 sentence narrative role in the current book's scope>
```

Examples in production:
- `content/en/matthew/PEOPLE.md`: `## Avraham (Abraham)` → **See:** genesis/PEOPLE.md → **In Matthew:** First in the genealogy (1:2)...
- `content/en/john/PEOPLE.md`: `## Mosheh (Moses)` → **See:** exodus/PEOPLE.md → **In John:** Referenced as the giver of the Torah (1:17)...

**Locale-translation table for label aliases** (the parser at `src/infrastructure/content/people-parser.ts` accepts these case-insensitively):

| Field | EN | PT-BR | DE | ES |
|---|---|---|---|---|
| `See` (crossBookSee) | `See` | `Ver` | `Siehe` | `Ver` |
| `In <Book>` (inBook) | `In Matthew`, `In Genesis`, `In John` (+ future books) | `Em Mateus`, `Em Gênesis`, `Em João` | `In Matthäus`, `In Genesis`, `In Johannes` | `En Mateo`, `En Génesis`, `En Juan` |

**Allow-list of valid `**See:**` target slugs (v3.3.2 published list):**

| Slug | Status |
|---|---|
| `genesis` | Currently authored — resolves to active link |
| `matthew` | Currently authored — resolves to active link |
| `john` | Currently authored — resolves to active link |
| `acts` | Forward-tracked — Phase 14+ — renders as plain-text fallback until authored |
| `exodus` | Forward-tracked — Phase 12+ Pentateuch — renders as plain-text fallback until authored |
| `kings` | Forward-tracked — Phase 14+ — renders as plain-text fallback until authored |
| `isaiah` | Forward-tracked — Phase 14+ — renders as plain-text fallback until authored |

Adding a new target slug to the allow-list is part of the new-book activation checklist below.

**When to use see-only stub vs. full canonical entry:**

- **See-only stub** when the person's canonical biographical material lives in another book (e.g., OT patriarchs appearing in Matthew 1's genealogy → see-only pointing to genesis), AND the current book's narrative role is limited to references or brief mentions (≤4 sentences fits the `**In <Book>:**` field comfortably).
- **Full canonical entry** when the person's substantive narrative arc lives in the current book, OR when the person's canonical-home book is not yet authored AND the current book has the most substantive material currently available (e.g., Rachav and Rut appear in Matthew's genealogy; their canonical OT homes Joshua and Ruth are not yet authored; matthew/PEOPLE.md carries full entries until those OT books land).

**New-book activation checklist — 5 SYNCHRONIZED CHANGES required when adding a new book's PEOPLE.md:**

1. **Author content** — `content/{en,pt-br,de,es}/{new-book}/PEOPLE.md` (all 4 locales).
2. **Extend bookLabels** — `src/app/[locale]/[book]/people/page.tsx`: add `{new-book}: t("book.{new-book}")` to the `bookLabels` map.
3. **Extend parser aliases** — `src/infrastructure/content/people-parser.ts` `EXACT_LABEL_ALIASES.inBook`: add 4 locale forms (e.g., `"in kings"`, `"em reis"`, `"in könige"`, `"en reyes"`). Without this, `**In {New Book}:**` fields will silently parse as `undefined`.
4. **Add i18n key** — `src/infrastructure/i18n/messages/{en,pt-br,de,es}.json`: add `people.inBook.{new-book}` translation string in each locale file.
5. **Extend lint allow-list** — `scripts/content-lint.sh` §0.12 rule: add `{new-book}` to the cross-book PEOPLE pointer allow-list (CORE Rule 29 v3.3.2 allow-list table above).

All five changes should land in the same commit or PR. The "Add new entries here" comment in `people/page.tsx` flags only step #2; the full sequence is documented here.

**Content-lint enforcement (warn-only §0.12):** `scripts/content-lint.sh` validates that every `**See:** {slug}/PEOPLE.md` (and locale aliases `Ver`/`Siehe`) refers to a slug in the allow-list above. Typos like `**See:** geneis/PEOPLE.md` produce a warn-only lint finding. The dangling-pointer UI fallback in `CrossBookSeeField` provides a second-layer defense at render time (plain text instead of broken link).

**Cross-book canonical-entry transition logging:** when a future authoring phase adds a full canonical entry that resolves an existing see-only stub (e.g., Phase 12 Genesis 13-50 adds full Yitschaq/Ya'aqov/Yosef bios, resolving the Matthew see-only stubs), the parent authoring entry's editorial-log entry should include a cross-reference line: `"Resolves see-only stubs at {book}/PEOPLE.md × 4 locales."` No separate transition log entry needed — the resolution is automatic via the parser + UI.
```

## Impact assessment

- **Affected signed-off verses:** zero (all current PEOPLE.md content is `provisional`).
- **Affected files (content):** zero (this is rules + parser/lint/UI infrastructure documentation; no content changes).
- **Affected files (rules):** `docs/rules/RULES-CORE.md` (extension of Rule 29 §People and Genealogy Files bold-paragraph block); CHANGELOG entry for v3.3.2.
- **Affected files (lint):** `scripts/content-lint.sh` (new §0.12 warn-only rule per Q1).
- **Affected files (CLAUDE.md):** `CLAUDE.md` (`## Architecture` paragraph per Q5).
- **Affected files (logs):** `docs/editorial-log/genesis.md` (Phase 13 closure entry); `docs/audit/PENDING.md` (README deferral per Q4=C; FEEDBACK item status).
- **Code changes:** none. `PersonEntry` type, `people-parser.ts`, `person-card.tsx`, `fs-content-repository.ts` all unchanged.
- **Test changes:** none. Existing 5 cross-book parser tests already cover the pattern.
- **Build/lint impact:** clean. Pre-execution baseline 819 tests / 2 lint warnings / build clean preserved.

## CHANGELOG entry (append to `docs/rules/CHANGELOG-v3.3.md`)

```markdown
## v3.3.2 emergency amendment — 2026-05-18

- **Target:** RULES-CORE.md Rule 29 §People and Genealogy Files bold-paragraph block.
- **Path:** Emergency amendment per RULES-CORE.md §AMENDMENT & LOCK PROTOCOL — bug fix / audit-driven formalization, no signed-off verses affected, additive clarification only.
- **Proposal artifact:** `docs/rules/proposals/v3.3.2-cross-book-PEOPLE-formalization.md`.
- **Change:** Formalized the cross-book see-only PEOPLE.md pattern that has been in production use since Phase 6 (2026-05-09). Documented the markdown convention (`**See:** {book}/PEOPLE.md` + `**In <Book>:** [narrative role]`), the locale-translation table for parser aliases, the v3.3.2-published allow-list of valid target slugs (genesis, matthew, john, acts, exodus, kings, isaiah), the see-only-vs-full-canonical decision criteria, the 5-change new-book activation checklist (content + bookLabels + parser aliases + i18n keys + lint allow-list), and the cross-book canonical-entry transition logging convention.
- **Trigger:** `docs/audit/PHASE_13_PLAN.md` (Q1=A / Q2=A / Q3=A / Q4=C / Q5=A decision lock). The audit absorption (`docs/audit/AUDIT_PHASE_13_PLAN.md`) identified 1 critical + 2 significant + 4 minor findings; all addressed pre-execution.
- **Content remediation:** zero — this is documentation / lint / CLAUDE.md only. No content files touched.
- **Editorial-log entry:** `docs/editorial-log/genesis.md` Phase 13 closure entry (date-based ID, anchor entry).
- **Verification:** 819 tests pass (unchanged baseline); `pnpm build` + `pnpm lint` + `pnpm content:lint` clean (2 expected warnings — §0.10 + §0.11 — plus new §0.12 warn-only rule which produces 0 warnings on current production content since all 7 allow-listed slugs are in use).
- **Project lead sign-off:** approved 2026-05-18 via the Q1/Q2/Q3/Q4/Q5 decision lock recorded in `docs/audit/PHASE_13_PLAN.md` §10.
```

## Decision

Approved 2026-05-18 per the project lead's lock of `docs/audit/PHASE_13_PLAN.md` Q1=A (Allow-list + §0.12 warn-only), Q2=A (Emergency amendment v3.3.2), Q3=A (Mention transitions in parent authoring entry), Q4=C (DEFER README to separate phase), Q5=A (CLAUDE.md paragraph).

The emergency-amendment criteria are satisfied:
- No signed-off verses affected (all PEOPLE.md content is provisional).
- All changes are additive (no existing rules modified).
- Bug fix / audit-driven hardening (formalizes a production-in-use pattern + closes a forward-tracking authoring trap).
- Written proposal artifact (this file) produced before content + lint changes land.
